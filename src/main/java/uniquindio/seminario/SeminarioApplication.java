package uniquindio.seminario;

import org.springframework.boot.SpringApplication;

public class SeminarioApplication {
    public static void main(String[] args) {
        SpringApplication.run(SeminarioApplication.class, args);
    }
}